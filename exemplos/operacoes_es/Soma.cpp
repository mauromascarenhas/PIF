// Programa Soma gerado automaticamente por PIFC

#include <string>
#include <cstdlib>
#include <iostream>

using namespace std;

int main(int argc, char *argv[]){
	double a, b, soma;
	cout << boolalpha << "Digite dois numeros:" << endl;
	cin >> a >> b;
	soma = a + b;
	cout << boolalpha << "A soma deles eh : " << soma << endl;
	return EXIT_SUCCESS;
}
